import { ButtonDecorationDirective } from './button-decoration.directive';

describe('ButtonDecorationDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonDecorationDirective();
    expect(directive).toBeTruthy();
  });
});
